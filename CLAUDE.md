# הנחיות פרויקט — דשבורדים משרד התרבות והספורט

מקור מחייב: `עקרונות עיצוב - משרד התרבות והספורט.docx`. **כל המוקאפים בפרויקט חייבים לציית לעקרונות הבאים.** בכל שינוי/יצירה של דשבורד — יישמו את המפרט הזה.

## מבנה דשבורד אחיד (מלמעלה למטה)
1. **לוגו** משרד התרבות והספורט בראש כל מסך.
2. **כותרת** הדשבורד.
3. **מסננים גלובליים** (filter bar) בחלק העליון.
4. **מדדי KPI** מרכזיים.
5. **גרפים וניתוחים** עיקריים.
6. **טבלאות פירוט** במידת הצורך.

פשטות · קריאות · עקביות בין כלל הדשבורדים.

## מיתוג וזהות חזותית
- לוגו בראש כל דשבורד/מסך.
- שפה עיצובית אחידה בכל הדשבורדים.
- **RTL מלא** בכל מסך (`dir="rtl"`).
- פונט: **"Amazon Ember"** (פונט מובנה של AWS QuickSight). במוקאפים — stack:
  `font-family: "Amazon Ember", "Heebo", system-ui, sans-serif;`

## פלטת צבעים (משתני CSS מחייבים)
```css
:root {
  /* ראשיים */
  --navy:   #1F3F6D;  /* מותג ראשי · כותרות · KPI · גרפים מרכזיים · גברים */
  --green:  #248A3D;  /* ערכים חיוביים · עמידה ביעדים · מגמות חיוביות */
  --red:    #DA3439;  /* התראות · חריגות · ירידה בביצועים */
  /* מידע / קטגוריות */
  --sky:    #2D9CDB;  /* נשים (עקבי בכל הדשבורדים) */
  --purple: #6B4FA0;  /* קטגוריה */
  --orange: #E07B39;  /* קטגוריה */
  --slate:  #7A8FA6;  /* קטגוריה */
  --gold:   #F8C33E;  /* קטגוריה */
  /* ממשק */
  --bg:        #F2F4F7;  /* רקע הדשבורד */
  --surface:   #FFFFFF;  /* רקע ויזואליזציות וכרטיסי KPI */
  --border:    #E5E9F0;  /* גבולות */
  --ink:       #1A1A2E;  /* טקסט ראשי + KPI */
  --ink-muted: #6B7280;  /* טקסט משני · כותרות משניות · הסברים */
}
```

### כללי שימוש בצבע (מחייב)
- רקע עמוד = `--bg` (#F2F4F7). כל כרטיס/ויזואליזציה/טבלה/KPI על `--surface` (#FFFFFF).
- `--navy` לכותרות, KPI וגרפים מרכזיים.
- **`--sky` תמיד = נשים · `--navy` תמיד = גברים** (אל תחליפו).
- `--green` למגמות חיוביות/הצלחות · `--red` לחריגות/התראות.
- Purple/Orange/Slate/Gold — לקטגוריות בלבד.

## טיפוגרפיה
- כותרות קצרות וברורות.
- תמיכה מלאה בעברית + RTL.
- להימנע מטקסטים ארוכים בתוך גרפים/ויזואליזציות.

## נגישות
- ניגודיות גבוהה בין טקסט לרקע.
- צבע לבדו לא מספיק — תמיד גם תוויות / Tooltips / Legend.
- גודל טקסט קריא בכל הרכיבים.

## תוויות נתונים (Data Labels)
- ב-QuickSight: להפעיל Data Labels בכל visual שבו הן קריאות (toggle מובנה עם מניעת חפיפות).
- **סגנון נקי — הילה, לא תיבה**: טקסט עם `text-stroke` דק (כהה+הילה לבנה על רקע בהיר; לבן+הילה כהה על מקטע צבעוני). בלי תיבות/מסגרות — הן מעמיסות.
- מוערמות/דונאט: תווית במרכז המקטע; מקטעים קטנים מוסתרים.
- **גרפים צפופים** (היסטוגרמות, ריבוי קווים): לא להציג תווית בכל נקודה — להסתמך על Tooltip, או להציג רק בנקודה האחרונה של כל קו.
- במוקאפים (Chart.js): `chartjs-plugin-datalabels`, פונט Amazon Ember.

## מגבלות פלטפורמה
היעד: **AWS QuickSight**. להישאר ביכולות המובנות שלו (סוגי ויזואליזציה: KPI · Bar · Stacked Bar · Clustered Bar · Line · Donut · Table) ולשמור אחידות/ביצועים.

## קבצי מוקאפ בפרויקט (להחיל עליהם)
`achievements.html` · `suggestions.html` · `index.html` · `dashboard.html` ·
`wg-women.html` · `wg-athletes.html` · `wg-roles.html` · `wg-scores.html` · `wg-mockup.html` · `ideas.html`

---
*כשמייצרים/משנים מוקאפ: התחילו מהמשתנים שלמעלה, אל תכניסו צבעים מחוץ לפלטה, ושמרו על המבנה האחיד.*

---
## TODO — research-swarm: לחזק עמידות ותיעוד (חובה להטמיע ב-skill בהיקף user)
מקור מלא: `הצגה למורן/api-research/SWARM-IMPROVEMENTS.md`. תוכנן 15/07/2026, טרם מומש. לבנות תחילה project-local, ואז **לקדם ל-skill ב-`~/.claude/skills/research-swarm`** (גם אם ההקשר יאבד — הכיוון חייב לשרוד).
- **#1 resume:** `STATE.md` נגזר (לא נערך ידנית) = עוגן הקשר לאורchestrator בין sessions; נקרא בהפעלה, נכתב מחדש אחרי merge.
- **#3 עמידות:** לכידה מצטברת — כל agent כותב checkpoint לכל מקור ל-`agents/<key>.part` (agents יכולים FS ב-Bash); סולם התאוששות: detect (journal started-vs-result) → retry → salvage → flag גלוי.
- **#2 שלמות:** שורת coverage לכל fetch עם `why` תמציתי (by construction, ~<1% tokens); backstop = diff provenance מול coverage.
- **דחוי (לא לבנות):** schema מועדף-לא-נכפה ל-decisions — להחליט אמפירית אחרי בחינת דוחות agents.
- **פריסה:** verbose (decisions/provenance/partials) מחוץ לנתיב הטעינה הדיפולטי; דיפולט = `STATE.md`+`findings.jsonl`.
